services:
  libavif-aom-container:
    image: libavif-aom-container
    container_name: libavif-aom-container
    build:
      context: .
      dockerfile: Dockerfile
    stdin_open: true
    tty: true
    volumes:
      - ./results:/workspace/results
      - ./imageset:/workspace/imageset
      - ./src:/workspace/src
